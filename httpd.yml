---
- name: Manage Apache on web servers
  hosts: all
  remote_user: root

  tasks:
    - name: Ensure Apache is at the latest version
      ansible.builtin.yum:
        name: httpd
        state: latest
  
    - name: Remove Apache if necessary
      ansible.builtin.yum:
        name: httpd
        state: absent
